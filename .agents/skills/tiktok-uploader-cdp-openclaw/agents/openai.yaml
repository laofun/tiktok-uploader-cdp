interface:
  display_name: "TikTok CDP OpenClaw"
  short_description: "Maintain CDP-only TikTok uploader with config-driven selectors and stable orchestrator contract"
  default_prompt: "Use the tiktok-uploader-cdp-openclaw skill to implement or review changes while preserving CDP-only auth, config.toml-driven selectors/timeouts, and machine-readable OpenClaw error semantics."
